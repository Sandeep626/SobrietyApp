<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proj">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/amethyst_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proj">
        <activity android:name=".HelpActivity"></activity>
        <activity android:name=".HowActivity" />
        <activity android:name=".WhyActivity" />
        <activity android:name=".CheckActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".AffectActivity" />
        <activity android:name=".AddictionActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SaveActivity" />
        <activity android:name=".MotvActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>